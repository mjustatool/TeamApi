scalar Date
type Query {
    getDataInserted(comp:String):[Team]
    getTeamsByChoice(comp:String):[Team]
}

type Team{
    id:Int
    teamName:String
    classment:Int
    matchPlayed:Int
    wins:Int
    matchDraw:Int
    defeat:Int
    points:Int
}
